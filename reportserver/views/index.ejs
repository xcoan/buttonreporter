<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Index</title>
  </head>
  <body>
    <h1>Indexed Crimestamps:</h1>
    <br />

    <% for(var i=0; i < crimestamps.length; i++) {%>
      <li class="crimes">
        <span><%= crimestamps[i].timestamp %>:</span>
        <span><%= crimestamps[i].source %></span>
      </li>
    <% } %>
  </body>
</html>
